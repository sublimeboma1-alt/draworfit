# Secret API key generated in the Chariow dashboard. Never expose it to React.
CHARIOW_API_KEY=sk_live_replace_me
# Secret of the Pulse configured for successful.sale.
CHARIOW_WEBHOOK_SECRET=replace_me
# Public root URL of this Django/React application (no trailing slash).
CHARIOW_REDIRECT_URL=https://draworfit.example.com
